Проведи ревью текущих незакоммиченных изменений (`git diff`) в этом проекте.

Проверь:
1. Соответствие стеку — TypeScript строгая типизация, стиль NestJS (модуль/сервис/резолвер).
2. Изменения `prisma/schema.prisma` сопровождаются миграцией (`prisma migrate dev`).
3. Новые GraphQL-поля описаны через `@Field` с корректными типами и `nullable`, где нужно.
4. Docker-сборка не сломана (учтены новые зависимости в `Dockerfile`).
5. Нет утечки секретов (`.env`, ключи) в диффе.

Выведи краткий список замечаний и предложи конкретные правки.
